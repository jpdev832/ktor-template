buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
    }
}

group 'ktor-template'
version '0.0.1'

task stage {
    dependsOn ":server:installDist"
}
